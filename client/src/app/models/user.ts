export class User {
  id: number;
  email: string;
  role: number;
  active: boolean;
  name: string;
  dateOfBirth: string;
  country: string;
  region: string;
  postalCode: string;
  phoneNumber: string;
}